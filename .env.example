# ============================================================
#  MDM Web Frontend — Environment Variables
#  Copy this to .env and fill in your values
#  ⚠  .env is in .gitignore — NEVER commit real secrets!
# ============================================================

# Backend API base URL (required)
# On Render, set this to your deployed backend URL
VITE_API_BASE_URL=https://childbackend.onrender.com/api
